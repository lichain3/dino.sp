#include <iostream>
#include <windows.h>
#include <math.h>
using namespace std;

int main()
{

    setlocale(LC_ALL, "Russian");
    int k, j;

    cout << " вадрат числа:  ";
    cin >> k;
    do
    {
        j = pow(k,2);
        cout << " вадрат числа" << k << " = " << j;
        cout << "¬ведите число";
        cin >> k;
        system("cls");
    } while (k!=981);
    
    return 0;
}
