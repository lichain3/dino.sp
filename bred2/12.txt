#include "pch.h"
#include <iostream>

using namespace std;

int main()
{


	char koob[6][5][5] = { {{'_', '_', '_', '_','_'},
							{'|', ' ', ' ', ' ','|'},
							{'|', ' ', 'O', ' ','|'},
							{'|', ' ', ' ', ' ','|'},
							{'_', '_', '_', '_','_'}},


							{{'_', '_', '_', '_','_'},
							{'|', ' ', ' ', ' ','|'},
							{'|', 'O', ' ', 'O','|'},
							{'|', ' ', ' ', ' ','|'},
							{'_', '_', '_', '_','_'}},


							{{'_', '_', '_', '_','_'},
							{'|', ' ', 'O', ' ','|'},
							{'|', ' ', ' ', ' ','|'},
							{'|', 'O', ' ', 'O','|'},
							{'_', '_', '_', '_','_'}},


							{{'_', '_', '_', '_','_'},
							{'|', 'O', ' ', 'O','|'},
							{'|', ' ', ' ', ' ','|'},
							{'|', 'O', ' ', 'O','|'},
							{'_', '_', '_', '_','_'}},


							{{'_', '_', '_', '_','_'},
							{'|', 'O', ' ', 'O','|'},
							{'|', ' ', 'O', ' ','|'},
							{'|', 'O', ' ', 'O','|'},
							{'_', '_', '_', '_','_'}},


							{{'_', '_', '_', '_','_'},
							{'|', 'O', ' ', 'O','|'},
							{'|', 'O', ' ', 'O','|'},
							{'|', 'O', ' ', 'O','|'},
							{'_', '_', '_', '_','_'}}, };


	int x, c, v, y, sum1, sum2, sr=1;

	x = 1 + rand() % 6;
	c = 1 + rand() % 6;

	v = 1 + rand() % 6;
	y = 1 + rand() % 6;

	setlocale(LC_ALL, "Russian");

	cout << "У бота выпало:\t\t\t" << "У вас выпало:\n";



	for (int i = 0; i < 5; i++)
	{
		for (int q = 0; q < 5; q++)
		{
			cout << koob[x - 1][i][q];
		}

		cout << "  ";

		for (int q = 0; q < 5; q++)
		{
			cout << koob[c - 1][i][q];
		}

		cout << "\t\t\t";

		for (int q = 0; q < 5; q++)
		{
			cout << koob[v - 1][i][q];
		}

		cout << "  ";

		for (int q = 0; q < 5; q++)
		{
			cout << koob[y - 1][i][q];
		}

		cout << "\n";
	}

	if ((c + x) > (y + v))
		sr = 0;

	y = 0;
	v = 0;

	for (int pop = 0; pop < 2; pop++)
	{
		if (sr == 0)
		{	cout << "Ход бота, в ведите любой символ:";
		cin >> x;

			for (int i = 0; i < 4; i++)
			{
				x = 1 + rand() % 6;
				c = 1 + rand() % 6;

				for (int i = 0; i < 5; i++)
				{
					for (int q = 0; q < 5; q++)
					{
						cout << koob[x - 1][i][q];
					}

					cout << "  ";

					for (int q = 0; q < 5; q++)
					{
						cout << koob[c - 1][i][q];
					}
					cout << "\n";
				}
				y += x + c;
				cout << "\n\n";
			}
			sr = 1;
		}


		else
		{	cout << "Ваш ход, в ведите любой символ:";
			cin >> x;
			for (int i = 0; i < 4; i++)
			{
				x = 1 + rand() % 6;
				c = 1 + rand() % 6;

				for (int i = 0; i < 5; i++)
				{
					for (int q = 0; q < 5; q++)
					{
						cout << koob[x - 1][i][q];
					}

					cout << "  ";

					for (int q = 0; q < 5; q++)
					{
						cout << koob[c - 1][i][q];
					}
					cout << "\n";
				}
				v += x + c;
				cout << "\n\n";
			}
			sr = 1;
		}
	}

	if (y > v)
		cout << "Бот победил, он набрал: " << y << "    очков.\nМежду вами разрыв: " << y - v;

	else if (y < v)
		cout << "Вы победили, бот набрал: " << y << "   очков.\nМежду вами разрыв: " << v - y;
	else cout << "Ничья";
	return 0;
}
